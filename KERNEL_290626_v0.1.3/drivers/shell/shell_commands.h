#ifndef SHELL_COMMANDS_H
#define SHELL_COMMANDS_H

/**
 * @brief Analisa e executa o comando digitado no prompt
 * @param cmd String contendo o comando e os argumentos
 */
void shell_execute_command(char* input_buffer);

#endif
